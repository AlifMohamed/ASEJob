apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Release.Name }}-job
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: python-job
        image: "localhost:5000/python-job:latest"
        imagePullPolicy: IfNotPresent