apiVersion: batch/v1
kind: Job
metadata:
  name: {{ .Release.Name }}-job
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: python-job-{{ values.job.id }}
        image: python-job:v2
        imagePullPolicy: IfNotPresent